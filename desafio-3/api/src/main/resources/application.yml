spring:
  datasource:
    url: jdbc:postgresql://redesocial-postgres:5432/redesocial
    username: redesocial
    password: redesocial
server:
  error:
    include-message: always
logging:
  level:
    org.hibernate.SQL: DEBUG
    org.hibernate.type.descriptor.sql.BasicBinder: TRACE
